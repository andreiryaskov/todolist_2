import React from 'react';
import TodoList from "./todoList";

const App = () => {
    return (
        <div className={'app_wrapper'}>
            <TodoList/>
        </div>
    );
};

export default App;